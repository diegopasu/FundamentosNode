const frutero = require('./frutas')

const {frutas,dinero} = require('./frutas')

frutas.forEach(item => {
   console.log(item)
   //console.count(item)
})

console.log(dinero)

// EJEMPLO DE HTTP//levantar un servidor